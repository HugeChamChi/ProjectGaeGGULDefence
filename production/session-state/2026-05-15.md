# 세션 상태 — 2026-05-15

> 작업 브랜치: `USW_Ingame`
> 작업자: USW

<!-- STATUS -->
Epic: 인게임 로직 완성
Feature: 토템 시스템 완성
Task: 토템 SO 생성 / 선택 UI / 회전 스프라이트 / 소환 구조 정리
<!-- /STATUS -->

---

## 오늘 완료된 작업

### 1. 컴파일 에러 수정
- `GameDataManager.cs`, `UpgradeManager.cs` — `using System.Threading;` 누락 추가

---

### 2. TotemData SO 40개 자동 생성
- 경로: `Assets/WorkSpace/USW/Data/TotemData/TD_4000.asset` ~ `TD_4039.asset`
- 구글 시트 (gid=660087267) CSV를 Python으로 파싱하여 생성
- 설정된 값
  - `totemId` : 4000~4039 (시트 매핑 키)
  - `totemName` : 한국어 이름
  - `tier` : Normal=0 / Rare=1 / Epic=2 / Legend=3
  - `isRotatable` : 시트 is_rotatable 값
  - 버프 수치 : 시트 정수 ÷ 100 (비율로 저장), food_amount만 원본 정수
  - `description` : 시트 effect 텍스트 (`{}` 제거)
  - `effectRange`, `attackDisabledRange` : 빈 리스트 — TotemEditor에서 설정 필요
  - `prefab` : null — 특수 토템만 연결 필요

---

### 3. 토템 회전 스프라이트 시스템
- **`TotemData.cs`** — `rotationSprites[4]` 필드 추가 (0°/90°/180°/270°)
- **`TotemBase.cs`**
  - `Awake()` 추가 → `GetComponentInChildren<SpriteRenderer>()` 캐싱
  - `SetTotemData(TotemData)` 추가 → 소환 시 SO 주입용
  - `Rotate()` 변경 — `transform.localEulerAngles` 회전 제거 → `UpdateSprite()` 호출로 교체
  - `UpdateSprite()` 추가 — `rotationSprites[step]` → 없으면 `icon` fallback
  - `OnPlaced()` — `UpdateSprite()` 호출 추가 (배치 시 초기 스프라이트 세팅)

---

### 4. 토템 소환 구조 정리

#### TotemSpawner.cs 간소화
- `totemPrefabs[]` 배열 제거
- `SpawnRandomTotem()`, `SpawnTotemByIndex()`, `GetTotemData()`, `ValidatePrefabs()` 제거
- `genericPrefab` 하나 추가 (Inspector 연결 필요)
- `SpawnTotemByData(TotemData data)` 만 남김
  - `data.prefab != null` → 해당 프리팹 사용 (특수 동작 토템)
  - `data.prefab == null` → `genericPrefab` 사용 + `SetTotemData(data)` 주입 (일반 토템)

#### BossRewardUI.cs
- 빈 클래스로 교체 (TotemSelectUI로 대체됨)
- Unity Editor에서 씬의 해당 GameObject 삭제 필요

---

### 5. 토템 선택 UI (보스 처치 보상)
- **신규 `TotemSelectCardUI.cs`** (`Assets/WorkSpace/USW/Scripts/IngameUI/`)
  - 카드 하나 (아이콘, 이름, 등급, 설명)
  - DOTween 확대 애니메이션 (Select/Deselect)
  - 등급별 색상 (Normal=#99c5ff / Rare=#c0a0f9 / Epic/Legend=#db9604)
- **신규 `TotemSelectUI.cs`** (`Assets/WorkSpace/USW/Scripts/IngameUI/`)
  - `InGameSingleton<TotemSelectUI>`
  - `Show(Action onChoiceMade)` — WaveManager에서 호출
  - 30초 타이머 (Realtime, 타임아웃 시 첫 번째 카드 자동 선택)
  - Time.timeScale=0 + PauseLoops/ResumeLoops
  - `totemPool[]` Inspector 배열 — TD_4000~TD_4039 등록 대상
  - 빈 셀 없으면 `fallbackFood` 지급
- **`WaveManager.cs`** — `OnSingleBossDefeated()` 변경
  - `GiveRandomTotem()` 제거 → `Manager.TotemSelect.Show(OnTotemSelectionDone)` 호출
- **`Manager.cs`** — `TotemSelect` 프로퍼티 추가

---

### 6. 특수 토템 스크립트 매핑 (분석 결과)

#### 기존 스크립트 재사용 가능 (프리팹 + prefab 연결 필요)
| SO | 이름 | 스크립트 |
|---|---|---|
| TD_4010 | 전략적 배치 | `TotemPositionBuff` |
| TD_4015 | 바람의 가호 | `TotemTimedBuff` (cycle=30, burst=10) |
| TD_4031 | 일시적 각성 | `TotemTimedBuff` (cycle=10, burst=5) |
| TD_4032 | 영혼의 제물 | `TotemSellStack` |
| TD_4033 | 결속의 힘 | `TotemProximityBuff` |
| TD_4034 | 전장의 지휘관 | `TotemDisableBoost` |
| TD_4035 | 제한 해제 | `TotemNullifyDebuff` |
| TD_4036 | 정점의 권능 | `TotemDisableBoost` |
| TD_4038 | 승전보 | `TotemBossKillStack` |
| TD_4039 | 공명의 울림 | `TotemTotemCount` |

#### GenericBuffTotem 근사치 처리 (추후 전용 스크립트 필요)
| SO | 이름 | 실제 효과 |
|---|---|---|
| TD_4007 | 백발백중 | 10회 공격마다 100% 치명타 |
| TD_4011 | 반복 타격 | 5회 공격 후 추가 데미지 |
| TD_4012 | 치명적인 약탈 | 치명타 시 식량 획득 |
| TD_4013 | 거인의 일격 | 공격력+, 공격속도 디버프 |
| TD_4014 | 과부하 | 공속=1 고정, 공속→공격력 전환 |
| TD_4022 | 잔상의 궤적 | 5회 공격 후 추가 1회 공격 |
| TD_4023 | 추격의 칼날 | 3회 공격마다 추가 데미지 |
| TD_4037 | 파괴적 전환 | 공격속도 증가를 공격력으로 전환 |

---

## Unity Editor 미완료 작업

- [ ] IngameScene에 `GameDataManager` GameObject 추가
- [ ] IngameScene에 `TotemSelectUI` 패널 추가 + Inspector 연결
  - `cardContainer`, `cardPrefab`, `confirmButton`, `selectionTimerText`
  - `totemPool[]` — TD_4000~TD_4039 등록
- [ ] IngameScene에 `TotemInfoPopupUI` 패널 추가 + Inspector 연결
- [ ] IngameScene에서 `BossRewardUI` GameObject 삭제
- [ ] `TotemSpawner` Inspector — `genericPrefab` 연결
  - GenericTotemPrefab 구성: `GenericBuffTotem` + `SpriteRenderer` + `DragHandler`
- [ ] 특수 토템 10개 전용 프리팹 생성 + SO `prefab` 필드 연결
- [ ] TD_4000~TD_4039 각 SO에 `icon` 및 `rotationSprites[0~3]` 스프라이트 연결
- [ ] TotemEditor에서 각 SO `effectRange` / `attackDisabledRange` 설정

---

## 수정된 파일 목록

| 파일 | 변경 내용 |
|---|---|
| `CoreSystem/Managers/GameDataManager.cs` | using System.Threading 추가 |
| `Upgrade/UpgradeManager.cs` | using System.Threading 추가 |
| `Totem/TotemData.cs` | rotationSprites[4] 추가, prefab 선택적 오버라이드로 주석 변경 |
| `Totem/TotemBase.cs` | Awake, SetTotemData, UpdateSprite 추가, Rotate 수정 |
| `Totem/TotemSpawner.cs` | genericPrefab 방식으로 전면 재작성 |
| `CoreSystem/Managers/WaveManager.cs` | GiveRandomTotem 제거, TotemSelectUI 호출로 교체 |
| `CoreSystem/Manager.cs` | TotemSelect 프로퍼티 추가 |
| `IngameUI/BossRewardUI.cs` | 빈 클래스로 교체 |

| 파일 (신규) | 내용 |
|---|---|
| `IngameUI/TotemSelectCardUI.cs` | 토템 선택 카드 UI |
| `IngameUI/TotemSelectUI.cs` | 토템 선택 패널 (30초 타이머) |
| `Data/TotemData/TD_4000.asset` ~ `TD_4039.asset` | TotemData SO 40개 |
